# Copy to .env.local and fill in values for local dev / reference for Vercel env vars.

# Mapbox (crime maps — optional until map UI ships)
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=

# Production canonical URL (e.g. https://your-app.vercel.app or custom domain)
# NEXT_PUBLIC_SITE_URL=https://your-app.vercel.app
